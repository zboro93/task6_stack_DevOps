apiVersion: v1
kind: Service
metadata:
  name: nato-service
  namespace: default
spec:
  selector:
    app: nato
  ports:
    - protocol: 'TCP'
      port: 80
      targetPort: 80
  type: LoadBalancer
